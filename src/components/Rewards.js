// src/components/Rewards.js
import React from 'react';

const Rewards = () => {
    return (
        <div>
            {/* Other reward-related content */}
            {/* Remove the Claim Rewards button from here if it exists */}
        </div>
    );
};

export default Rewards;